comment:
  layout: "diff, flags, files"

flags:
  statetests:
    paths:
      - lib/evmone
      - test/state
      - test/statetest
  blockchaintests:
    joined: false
    paths:
      - lib/evmone
